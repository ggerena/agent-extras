interface:
  display_name: "Auto PR Review"
  short_description: "Open a PR and request agent review"
  default_prompt: "Use $auto-pr-review to verify this implementation, open a PR, and request review from another agent before merge."
